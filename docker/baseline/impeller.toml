[server]
host = "0.0.0.0"
port = 8080

[metrics_server]
host = "0.0.0.0"
port = 9991

[model_manager]
search_paths = ["/models"]

[features]
dev = true
streaming_ner = true
default_batch_size = 1
warmup_gpu = false

[global]
device = "cpu"

[health]
gpu_required = false

[flux]
enabled = false
